#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;
    if (x != y ) {
        cout << "x not y" << endl;
    }

    if ((x == y )) {
        cout << "x is y" << endl;
    }
}
